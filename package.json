{
  "private": true,
  "name": "morph-motion-workspace",
  "workspaces": ["preview/*"],
  "scripts": {
    "dev": "npm run dev -w shadcn-demo",
    "build": "npm run build -w shadcn-demo",
    "typecheck": "npm run typecheck -w shadcn-demo",
    "lint": "npm run lint -w shadcn-demo",
    "registry:build": "npx shadcn@latest build"
  }
}
