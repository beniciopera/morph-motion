{
  "private": true,
  "name": "morph-motion-workspace",
  "workspaces": ["packages/*", "playground"],
  "scripts": {
    "build": "npm run build -w morph-motion",
    "dev": "npm run dev -w playground",
    "build:lib": "npm run build -w morph-motion",
    "dev:playground": "npm run dev -w playground"
  }
}
